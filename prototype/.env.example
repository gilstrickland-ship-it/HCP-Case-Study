# Anthropic API key — required for live Claude classification/composition.
# Without it, the app runs on deterministic scripted fallbacks (demo still works).
ANTHROPIC_API_KEY=sk-ant-...

# Optional model overrides.
AI_MODEL_CLASSIFY=claude-haiku-4-5
AI_MODEL_COMPOSE=claude-sonnet-5
